<section id="warehouse-show" class="container-fluid my-3">
	<div class="row">
		<div class="col-10 mx-auto">
			<h1 class="display-3 text-center">Anaqueles</h1>
			<div class="container-fluid">
				<div class="row">
					<% @shelves.each do |shelf| %>
					<%= link_to shelf_show_path(shelf.id), class: "col-12 col-sm-6 col-md-4 col-xl-3 btn-green text-decoration-none" do %>

						<p class="pt-3 text-center m-0"><i class="bi bi-bookshelf text-black">
						</i> <%= shelf.code %></p>
						<p class="m-0 text-center pb-3">Categoría: <%= shelf.category %> </p>

					<% end %>
					<% end %>
				</div>
			</div>
		</div>
	</div>
</section>